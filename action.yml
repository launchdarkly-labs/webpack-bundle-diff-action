name: 'Webpack stats diff'
description: 'Adds a comment to your pull request with a summary of webpack stats changes between two builds'
inputs:
  base-stats-path:
    description: 'The path to the json stats file for the base branch'
    required: true
  pr-stats-path:
    description: 'The path to the json stats file for the pull request branch'
    required: true
  github-token:
    description: 'The Github token used to comment on pull requests'
    required: true
runs:
  using: 'node12'
  main: 'dist/index.js'
